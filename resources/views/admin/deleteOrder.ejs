<section class="login flex justify-center pt-24">
    <div class="w-full max-w-lg">
        <form action="/admin/delete-order" method="POST" class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
          <% if(messages.error) { %>
            <span class="text-red-500 text-sm"><%= messages.error %></span>
          <% } %>
          <div class="mb-4">
            <label class="block text-gray-700 text-sm font-bold mb-2" for="order_id">
              Order Id
            </label>
            <input
              name="order_id" value="<%= order_id %>" required
              class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" id="order_id" type="text" placeholder="Enter Order Id">
          </div>
          <div class="flex items-center justify-between">
            <button 
              class="btn-primary rounded text-white font-bold py-2 px-4 focus:outline-none focus:shadow-outline" 
              style="background: rgb(196, 49, 49);"
              type="submit"
            >
              Delete order
            </button>
            <a class="inline-block align-baseline font-bold text-sm text-blue-500 hover:text-blue-800" href="/admin/menuItems">
              View menu
            </a>
            
          </div>
          
        </form>
        <p class="text-center text-gray-500 text-xs">
          &copy;2021 Sarthak Shukla. All rights reserved.
        </p>
      </div>
</section>
